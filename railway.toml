[build]
builder = "nixpacks"
buildCommand = "pip install -r backend/requirements.txt"
buildEnv = { PYTHON_VERSION = "3.11.6" }

[start]
startCommand = "uvicorn backend.app.main:app --host 0.0.0.0 --port $PORT"